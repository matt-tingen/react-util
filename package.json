{
  "name": "@matt-tingen/react-hooks",
  "version": "0.0.9",
  "repository": "git@github.com:matt-tingen/react-hooks.git",
  "author": "Matt Tingen <matt.tingen@gmail.com>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc -p tsconfig.json",
    "lint": "eslint --ext .ts,.tsx src",
    "ts": "tsc -p tsconfig.json --noEmit --watch",
    "prepare": "yarn lint && yarn build"
  },
  "peerDependencies": {
    "react": "^16.13.1",
    "react-use": "^14.2.0"
  },
  "devDependencies": {
    "@matt-tingen/eslint-config": "^0.4.1",
    "@types/react": "^16.9.34",
    "eslint": "^8.8.0",
    "react": "^16.13.1",
    "react-use": "^17.1.1",
    "typescript": "^4.2.2"
  }
}
